---
paths:
  - "data/**"
---

# Data Files

## Skills (`data/skills.ts`)

- Adding a new skill category requires adding translation keys in BOTH `messages/en.json` and `messages/fr.json` under `Skills.categories.<key>`.
